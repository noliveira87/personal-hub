{
  "name": "personal-hub-monorepo",
  "version": "1.1.0",
  "description": "Unified personal management hub",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/hub",
    "packages/libs/ui"
  ],
  "scripts": {
    "dev": "npm --prefix packages/hub run dev -- --port 3000",
    "build": "npm --prefix packages/hub run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1",
    "dotenv": "^17.3.1"
  }
}
