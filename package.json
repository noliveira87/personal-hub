{
  "name": "personal-hub-monorepo",
  "version": "1.0.0",
  "description": "Unified personal management hub",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/home-contracts",
    "packages/libs/ui"
  ],
  "scripts": {
    "dev": "npm --prefix packages/home-contracts run dev -- --port 3000",
    "build": "npm --prefix packages/home-contracts run build"
  }
}
